package com.bookworm.backend.aspect;

// Superseded by com.bookworm.backend.service.impl.ServiceAspectsTest - that
// version places its fixture class inside the com.bookworm.backend.service.impl
// package so it actually matches these aspects' execution(...) pointcuts;
// a fixture class in this (com.bookworm.backend.aspect) package would silently
// never be advised, making the assertions meaningless. Left as an empty
// compilation unit (no delete tool available in this session) rather than a
// duplicate/misleading test.
